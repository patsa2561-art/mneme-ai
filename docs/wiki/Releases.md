# Releases

> Version-by-version highlights. The full per-commit detail lives in the [CHANGELOG](https://github.com/patsa2561-art/mneme-ai/blob/main/CHANGELOG.md).

═══════════════════════════════════════════════════════════════════════════════

## Latest

| Version | Headline | Wiki page |
|---|---|---|
| **v0.26.0** | **Super Pipeline + Iris Adoption + AI Teacher** — deeply-pipelined-superscalar engine with MPE eigentrust math (world-first) · 5 commands migrated to Iris journalist-pyramid · Mneme positioned as the teacher of AI. 1331 tests passing. | [[Super-Pipeline]] · [[AI-Teacher]] |
| v0.25.0 | **Iris + Regression Wall** — journalist-grade output engine (pyramid + AI headline + visual continuity + adaptive verbosity + 30-sec contract) + 179 new regression tests (help/no-throw/shape/snapshots). 1291 tests passing. | — |
| v0.24.0 | **Hierarchical Memory** — world-first compression-as-storage. 50K commits fit in one Claude prompt. `mneme htc-build` + `htc-stats`. 10× fewer tokens per query. Compressed MCP responses. | [[Hierarchical-Memory]] |
| v0.23.0 | **Speculative Reasoning** — streaming thought events (`--stream`) · Leviathan citation verifier · DDTree commit-tree search · ConstraintPruner trait · path-aware sessions · wisdom auto-adapt | [[Speculative-Reasoning]] |
| v0.22.2 | `mneme upgrade` — bulletproof self-update (bypasses npm cache + PATH conflicts) | — |
| v0.22.1 | **ResilientEnricher** — self-healing free-LLM fallback chain (Ollama → Groq → Together → OpenRouter → OpenAI). Per-provider health tracking + cooldown windows. | — |
| v0.22.0 | **Free Forever** — `mneme setup-free` 30-second wizard. Multi-provider auto-detect. Qwen 2.5 / Gemma 2 / Llama 3.3 curated. No API key required. | — |

═══════════════════════════════════════════════════════════════════════════════

## Earlier 2026 (foundation)

| Version | Headline |
|---|---|
| v0.21.1 | File paths in 9 commands — "where in the codebase?" |
| v0.21.0 | **Plain English Everything** — 32 commands humanized. No more `σ`, `robust z`, `MAD` jargon — every output translated for non-statisticians. |
| v0.20.2 | `forensics anomaly` reading guide + plain-English axis notes |
| v0.20.1 | Cross-platform `mneme guard` (Windows path fix) |
| v0.20.0 | `mneme do` smart dispatcher (7 flows) + `mneme guard` pre-commit hook |
| v0.19.x | **Zero-Install** — bundled WASM model + 4-tier auto-fallback ladder. `npm i -g mneme-ai && mneme index` works on any machine without setup. |
| v0.18.x | UI primitives unification + smart intelligence layer (every command renders through one design system) |
| v0.17.0 | **Forensic Code Science** — Bayesian author attribution + ENFSI verbal scale + CWE-aligned vulnerability hunt + insider-threat anomaly detection |
| v0.16.0 | **Guardian** — 24/7 self-healing engine + 4 novel scoring algorithms |
| v0.15.0 | Polish + index quality auditor |
| v0.14.0 | Audit-grade hallucination guard (zero-hallucination mode) |
| v0.13.0 | "Black Sheep" close every landscape gap |
| v0.12.0 | DNA · Drift · Chronicle · Oracle · Constellation |
| v0.11.0 | Time Machine + Pre-mortem + Ghost (3 world-firsts) |
| v0.10.0 | Initial Tier-2 quant analysis |
| v0.9.0  | Wisdom Mutant engine + insights + Obsidian export |
| v0.8.x  | First public release on npm + MCP Registry |

═══════════════════════════════════════════════════════════════════════════════

## Source of truth

- [CHANGELOG.md](https://github.com/patsa2561-art/mneme-ai/blob/main/CHANGELOG.md) — full per-version commit notes
- [GitHub Releases](https://github.com/patsa2561-art/mneme-ai/releases) — tags + downloadable artifacts
- [npm package](https://www.npmjs.com/package/mneme-ai) — published versions

`mneme upgrade` (v0.22.2+) is the recommended way to move between versions — it bypasses npm cache + diagnoses PATH conflicts that silently keep you on an old binary.
