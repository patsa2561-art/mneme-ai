# Twitter / X — 6-tweet thread draft

> Thread structure: hook → problem → demo → numbers → architecture → CTA.
> Attach the demo GIF to tweet 1.

---

**Tweet 1/6 — the hook (with GIF attached)**

```
Code knows what it does.
Git knows why.
The pager knows what broke.

Until Mneme, nothing connected them.

A local-first memory layer for your codebase that any AI assistant can query 👇
```

> Attach: 30-second demo GIF generated by `vhs demo.tape`

---

**Tweet 2/6 — the problem**

```
The problem with AI coding assistants today isn't intelligence — it's memory.

Ask Claude/Cursor "why does this retry?" and it looks at the current file and *guesses*. It doesn't read the PR from 8 months ago that explains the Stripe bug.

It hallucinates. You ship.
```

---

**Tweet 3/6 — the fix (one-liner)**

```
Mneme indexes your git history (commits + PR/issue bodies + blame), embeds it, and serves it via MCP.

Now Claude/Cursor/Continue/Copilot can ask the repo what it remembers.

$ mneme ask "why does parseAmount use try/catch around toString?"
   → PR #482, alice@, 2024-08-12: BigInt overflow on Stripe webhook
```

---

**Tweet 4/6 — the numbers**

```
Quality is measured, not claimed.

  recall@1     86.7%
  recall@3     86.7%
  MRR          90.0%
  query p50    1.2ms

Eval harness with golden set + recall@k/MRR/nDCG ships in-tree. CI blocks merges that regress any metric.

`npm run status` regenerates STATUS.md from a real run.
```

---

**Tweet 5/6 — under the hood**

```
Stack:
  • TypeScript monorepo
  • better-sqlite3 + FTS5 + BLOB embeddings
  • Hybrid retrieval: BM25 + vector cosine fused via Reciprocal Rank Fusion
  • Ollama default (local) → OpenAI (optional) → hash fallback (zero deps)
  • MCP server via the official SDK
  • CI on Win/macOS/Linux × Node 20/22

MIT.
```

---

**Tweet 6/6 — the CTA**

```
Try it on any git repo:

  npx -y mneme-ai init
  npx -y mneme-ai index
  npx -y mneme-ai ask "..."

  npx -y mneme-ai wisdom    ← bonus: 13 meditations on memory and code

⭐ github.com/patsa2561-art/mneme-ai

Honest feedback welcome — especially from anyone working on phase-3 incident correlation.
```

---

## Engagement playbook

- **Pin the thread** to your profile for 2 weeks after launch
- **QT influential dev voices** if they retweet — keeps it alive
- **Reply within 1 hour** to keep the algorithm boost
- **Don't end with #buildinpublic** — it tags you as marketing-first; let the work speak
- **Include `μνήμη`** in your bio after launch — it's a conversation starter

## What to avoid

- ❌ All-caps anywhere
- ❌ "🚀" or "🔥" — every dev tool overuses these
- ❌ "Game-changer" / "revolutionary" — instant credibility loss
- ❌ Tweeting and disappearing — first hour matters most
